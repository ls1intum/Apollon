import { styled } from '../theme/styles';

export const CanvasContainer = styled.svg`
  position: absolute;
  top: 0;
  left: 0;
  min-width: 50%;
  min-height: 50%;
  overflow: visible;
  transform: translate(100%, 100%);
`;
