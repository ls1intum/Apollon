import { SfcContainer } from '../base/sfc-container';
import { UMLElementType } from '../../uml-element-type';

/**
 * Represents a step element in a sfc.
 */
export class SfcStep extends SfcContainer {
  type = UMLElementType.SfcStep;
}
