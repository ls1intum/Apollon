import styled from 'styled-components';

export const Color = styled.div`
  flex: 0 0 230px;
  padding: 0 10px;
  height: 100%;
  min-height: inherit;
  max-height: inherit;
  overflow: auto;
  display: flex;
  flex-direction: column;

  svg {
    display: block;
    margin-left: auto;
    margin-right: auto;
  }
`;
