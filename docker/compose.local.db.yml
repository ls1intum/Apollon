services:
  db:
    image: redis/redis-stack-server:7.4.0-v0
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - dbdata:/data
    command:
      - redis-stack-server
      - --appendonly
      - "yes"
      - --appendfsync
      - everysec

volumes:
  dbdata:
