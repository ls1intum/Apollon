// The editor's icon button is the shared @tumaet/ui primitive, re-exported so the
// editor and webapp render one component ({ariaLabel, tooltip, children} API).
// Styling ships in the bundled, Tailwind-free components.css
// (data-slot="icon-button").
export { IconButton } from "@tumaet/ui/components/icon-button"
export type { IconButtonProps } from "@tumaet/ui/components/icon-button"
