---
"@tumaet/server": patch
---

Server-side diagram image export (SVG/PNG/PDF) now keeps edges that attach to the new grid-aligned connection points, deriving its render handles from the same connection model as the editor so exported edges land exactly where they do on the canvas.
