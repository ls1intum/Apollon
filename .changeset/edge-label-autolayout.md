---
"@tumaet/apollon": patch
---

Edge and interface labels now lay out neatly and consistently. Relationship and stereotype labels sit centered just above their edge and step aside when they would otherwise overlap a connected node or another edge; role and multiplicity labels stay on the correct side of bent edges; and communication-diagram message arrows point the right way. Provided/required interface names in component and deployment diagrams now sit centered below the interface symbol (and wrap if long) instead of floating off its top-right corner. Labels are now placed identically in the editor and in PNG/PDF/SVG exports.
