# Changesets

`npm run changeset` adds a file here for each user-visible PR. Each file declares which packages bump and contains the one-sentence changelog entry, verbatim. Configuration lives in [`config.json`](config.json); writing rules live in [`docs/development/release-notes.md`](../docs/development/release-notes.md).
