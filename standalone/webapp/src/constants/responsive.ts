// Width-only "narrow" breakpoint (portrait phones). Deliberately does NOT match
// phone-landscape, which is wide enough to keep the full desktop action set
// (just shorter) rather than collapse to the compact pills. Used by the editor
// header to choose compact pills vs. the full action bar.
export const NARROW_VIEW_QUERY = "(max-width: 767.95px)"
