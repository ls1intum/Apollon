export const secondary = "#A3A6A8"
// Dark chrome plate behind the navbars (home + editor) and the version-history
// sidebar. Toned to the same cool blue-slate hue (~220) as the editor surfaces
// (--apollon-background #181a1f, --home-surface-raised #20232a) so the whole UI
// reads as one temperature instead of the chrome sitting a touch more cyan.
export const NAVBAR_BACKGROUND_COLOR = "#20242c"
